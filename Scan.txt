Scan()
--> transactionProcessor.apply(currentBlock)
    --> currentBlock.apply()
        --> Account.addOrGetAccount(getGeneratorId())
    --> foreach(transaction)
        --> transaction.apply()
            --> senderAccount = tx.getSenderAccount()
            --> senderAccount.apply(senderPublicKey, block.height)
            --> Account.addOrGetAccount(recipientId)
            --> type.apply(tx, senderAccount, recipientAccount)
                --> TransactionType.Payment.apply()
                    --> senderAccount.addToBalanceNQT()
                    --> applyAttachment()